import { NgModule } from '@angular/core';
import { RouteCustomPipe } from './route-custom.pipe';

@NgModule({
  exports: [RouteCustomPipe],
  declarations: [RouteCustomPipe],
})
export class AttendModule {}
